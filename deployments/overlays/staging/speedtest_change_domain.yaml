apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: speedtest-tls
spec:
  rules:
    - host: beta.speedtest.nilsherzig.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: speedtest-service
                port: # TODO why do i need to overwrite this, it doesnt change
                  number: 80
  tls:
    - hosts:
        - beta.speedtest.nilsherzig.com
      secretName: beta-tls-speedtest-cert
