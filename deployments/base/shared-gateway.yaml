apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: shared-gateway
  namespace: infra
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  gatewayClassName: cilium
  listeners:
    - name: https-listener-argocd-dev-nilsherzig-com
      hostname: "argocd.dev.nilsherzig.com"
      protocol: HTTPS
      port: 443
      tls:
        mode: Terminate
        certificateRefs:
          - name: argocd-dev-nilsherzig-com
      allowedRoutes:
        namespaces:
          from: Selector
          selector:
            matchLabels:
              kubernetes.io/metadata.name: argocd

    - name: https-listener-nilsherzig-com
      hostname: "nilsherzig.com"
      protocol: HTTPS
      port: 9090
      tls:
        mode: Terminate
        certificateRefs:
          - name: nilsherzig-com
      allowedRoutes:
        namespaces:
          from: All
          selector:
            matchLabels:
              kubernetes.io/metadata.name: homepage
---
apiVersion: v1
kind: Namespace
metadata:
  name: infra
