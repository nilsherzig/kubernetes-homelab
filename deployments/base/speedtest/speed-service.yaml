apiVersion: v1
kind: Service
metadata:
  name: speedtest-service
  namespace: random-workloads-2
spec:
  selector:
    app.kubernetes.io/name: speedtest-pod
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
